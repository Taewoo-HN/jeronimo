spring:
  application:
    name: finale
  datasource:
    url: jdbc:mariadb://172.20.160.1:3306/finaldb
    username: root
    password: ${db-password}
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    lettuce:
      port: 6379
      host: localhost
server:
  port: 8028
  servlet:
    encoding:
      force: true

